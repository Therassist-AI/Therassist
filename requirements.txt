# Main dependency - manages tensorflow/tf-keras automatically
deepface

# Force compatible opencv version (4.10.x works with numpy 1.x)
opencv-python<4.11.0

# Ensure numpy 1.x for TensorFlow compatibility
numpy<2.0.0

# Deep learning - tf-keras provides compatibility layer for DeepFace
tf-keras

# Backend API
fastapi
uvicorn[standard]
python-dotenv
python-multipart

# Local speech-to-text transcription (no API key needed)
openai-whisper

# LLM API client (OpenAI-compatible, used for DeepSeek)
openai
ffmpeg-python
